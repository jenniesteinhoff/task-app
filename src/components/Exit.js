import React from "react";

export const Exit = () => {

  return (
  <div className="exit-container">
  <p>Thank you for using the task app. <br/>
    You can now close the window.</p>
  </div>  
  );
};